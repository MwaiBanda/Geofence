pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Geofence-Multiplatform"
include(":GeofenceAndroid")
include(":GeofenceSDK")